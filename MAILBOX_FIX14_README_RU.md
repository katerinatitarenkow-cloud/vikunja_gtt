# FIX14 — личный почтовый ящик пользователей

Устанавливать поверх полного FIX13 с заменой файлов.

Добавлено:
- отдельная страница `Почта` в левом меню для каждого авторизованного пользователя;
- Входящие / Отправленные;
- личные сообщения пользователь → пользователь;
- поиск получателя по имени или логину;
- тема и текст письма;
- ответы на сообщения;
- прочитано / непрочитано;
- счётчик непрочитанных внутри почтового ящика;
- поиск по теме и тексту;
- постраничный список;
- удаление только из собственного ящика: у второго участника копия остаётся;
- публичный link-share не имеет доступа к почте;
- email пользователей через API почты не раскрывается.

Новая миграция: `20260820150000`
Новая таблица: `user_mailbox_messages`

После распаковки:

```powershell
Get-ChildItem -Recurse | Unblock-File
.\BUILD-WINDOWS-FIXED-v2.ps1
```

Существующую SQLite-базу удалять не нужно.
